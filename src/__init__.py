"""
Investmint analytics package.

This file makes `src` importable as a regular Python package, so modules can be referenced
as `src.<module>` (e.g., `src.reporting`) reliably across tools like Streamlit.
"""

